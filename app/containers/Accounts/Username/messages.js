/*
 * Username Messages
 *
 * This contains all the text for the Username container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.Username";

export default defineMessages({
   header: {
      id: `${scope}.header`,
      defaultMessage: "This is the Username container!",
   },
});
