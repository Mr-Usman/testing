/*
 * Password Messages
 *
 * This contains all the text for the Password container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.Password";

export default defineMessages({
   header: {
      id: `${scope}.header`,
      defaultMessage: "This is the Password container!",
   },
});
