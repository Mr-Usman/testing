// import { selectUpdatePasswordDomain } from '../selectors';

describe('selectUpdatePasswordDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
